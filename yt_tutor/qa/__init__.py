"""Read layer: turn the stored video into things an agent can consume — the full
digest, FTS search, and timestamped evidence for questions. No LLM calls here;
the consuming agent does the reasoning."""
